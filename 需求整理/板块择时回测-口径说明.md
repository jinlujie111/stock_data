# 板块择时回测 — 口径说明（2026-08）

## 成交模型（冻结）

| 项 | 约定 |
|----|------|
| 信号确认 | 交易日**收盘**后写入 `signal_type=buy/sell` |
| 买入成交 | **T+1 开盘**（信号日下一交易日 `open`） |
| 卖出成交 | **T+1 开盘** |
| 未平仓 | 回测窗口末日按**收盘**盯市（`is_open=1`） |
| 成本 | `TimingConfig.cost_bps` 单边基点（默认 **3bp**）；买卖各扣一次 |
| 收益率 | `(exit/entry - 1) - 成本`；板块 `total_return` 为逐笔**复利** |
| 成功率 | 盈利已平仓笔数 / 已平仓笔数（不含盯市） |
| 夏普 | 已平仓收益均值 / 标准差（交易级，未年化） |
| Calmar | 总收益 / 最大回撤 |
| 买入持有 | 回测窗口首末收盘比；超额 = 策略 − 买入持有 |

## 图上标注

- 三角：信号日（收盘确认）
- 圆点：T+1 开盘成交（或窗口末盯市）

## 表

- `dwm_board_timing_signal_arch` — 热表 purge 前归档
- `dwm_board_timing_bt_run` / `_trade` / `_metrics`

扩展指标迁移：`mysql_tables/migrations/20260805_board_timing_bt_metrics_ext.sql`

## 命令

```bash
# 建表
mysql -u root -p < mysql_tables/migrations/20260805_board_timing_backtest.sql
mysql -u root -p < mysql_tables/migrations/20260805_board_timing_bt_metrics_ext.sql

# 信号日批后跑回测
bash dw-dwm/pro_dwm_board_timing_backtest.sh 20260805

# 参数网格（多 run_code）
python -m etl.board_timing.backtest 20260805 --grid
```

## Web

- 工作台：`/dc/board-timing`（参数面板 + Run 对照 + 权益双曲线）
- API：`/api/v1/timing/config`、`/backtest/runs`、`POST /backtest/run`、summary/metrics/trades
